import { createSystem, defaultConfig } from '@chakra-ui/react';

// No custom tokens yet: no design language exists until the product concept
// is decided. This exports Chakra's default system as-is.
export const chakraSystem = createSystem(defaultConfig);
